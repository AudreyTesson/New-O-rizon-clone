{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<div class="container mx-auto">

<div class="flex min-h-full flex-col justify-center px-6 py-12 lg:px-8 bg-blue-100">
    <div class="sm:mx-auto sm:w-full sm:min-w-sm">
        <img class="sm:w-96 md:w-1/2 sm:h-1/2 sm:mx-auto" src="https://images.pexels.com/photos/134643/pexels-photo-134643.jpeg" alt="photo d'une ville de nuit avec de grands immeubles et deux routes à deux voies chacune">

        <h2 class="my-5 mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Inscription</h2>
    </div>
    <div class="flex justify-center">

        <div class="flex justify-center bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4 w-96">

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
            <button class="my-3 px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded">{{ button_label|default('Sauvegarder') }}</button>
        {{ form_end(form) }}
        
        </div>
    </div>
</div>

{% endblock %}
