 {# ! TODO js pour fermeture de la div au click #}
 <div class="flex justify-center">
 {% for message in app.flashes("success") %}
    <div class="flex p-4 mx-4 justify-center lg:w-1/2 text-md text-green-800 rounded-lg bg-green-50 dark:bg-gray-800 dark:text-green-400" role="alert">
        <div>
            <span class="font-medium">{{ message }}</span> 
        </div>
    </div>
{% endfor %}
</div>